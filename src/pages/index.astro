---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<title>Intro</title>
		<style>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			body {
				font-family: 'Courier New', monospace;
				background: #000;
				color: #fff;
				min-height: 100vh;
				display: flex;
				justify-content: center;
				align-items: center;
			}

			.intro-container {
				text-align: center;
				max-width: 600px;
				padding: 40px 20px;
			}

			h1 {
				font-size: 2.5em;
				font-weight: 300;
				letter-spacing: 2px;
				margin-bottom: 20px;
				animation: typewriter 2s steps(30, end);
				border-right: 3px solid #fff;
				overflow: hidden;
				white-space: nowrap;
			}

			.subtitle {
				font-size: 1em;
				font-weight: 300;
				letter-spacing: 1px;
				margin-bottom: 40px;
				opacity: 0.7;
				animation: fadeIn 1.5s ease-out 1.5s both;
			}

			.divider {
				width: 40px;
				height: 1px;
				background: #fff;
				margin: 40px auto;
				animation: fadeIn 1.5s ease-out 2s both;
			}

			.content {
				font-size: 0.95em;
				line-height: 1.8;
				opacity: 0.6;
				animation: fadeIn 1.5s ease-out 2.5s both;
			}

			.dot-loader {
				display: inline-block;
				margin-top: 30px;
			}

			.dot-loader span {
				display: inline-block;
				width: 6px;
				height: 6px;
				background: #fff;
				border-radius: 50%;
				margin: 0 4px;
				animation: blink 1.4s infinite;
			}

			.dot-loader span:nth-child(2) {
				animation-delay: 0.2s;
			}

			.dot-loader span:nth-child(3) {
				animation-delay: 0.4s;
			}

			@keyframes typewriter {
				0% {
					width: 0;
				}
				100% {
					width: 100%;
				}
			}

			@keyframes fadeIn {
				from {
					opacity: 0;
				}
				to {
					opacity: 0.7;
				}
			}

			@keyframes blink {
				0%, 100% {
					opacity: 0.3;
				}
				50% {
					opacity: 1;
				}
			}

			@media (max-width: 768px) {
				h1 {
					font-size: 1.8em;
				}

				.subtitle {
					font-size: 0.9em;
				}
			}
		</style>
	</head>
	<body>
		<div class="intro-container">
			<h1>Coming Soon</h1>
			<p class="subtitle">Portfolio Under Development</p>
			<div class="divider"></div>
			<p class="content">Something interesting is being built here.</p>
			<div class="dot-loader">
				<span></span>
				<span></span>
				<span></span>
			</div>
		</div>
	</body>
</html>
